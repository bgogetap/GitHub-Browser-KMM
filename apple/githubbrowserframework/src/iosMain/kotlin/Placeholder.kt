/**
 * A source file is required for a target to build, so we include this here even though this target
 * is only meant to bring together all xplat modules for the iOS framework.
 */
class Placeholder {

    fun placeholder() {}
}